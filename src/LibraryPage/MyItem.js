import React from "react";

const MyItem = (props) => {
  // props.item.category can be "nasalibrary"(media_type: "image", "vedio"), "marsrover"
  return (
    <div>

    </div>
  )
}

export default MyItem